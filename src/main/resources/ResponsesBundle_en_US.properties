StatusResponse = Your app status is ${status}.
ThresholdResponseLine1 = You have ${count} thresholds in your app.
ThresholdResponseLineN = Threshold ${index} named ${name}, with ${status} status, and its value is ${value}.